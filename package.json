{
  "name": "mintwise",
  "version": "1.0.0",
  "private": true,
  "description": "Mintwise expense and savings tracker",
  "scripts": {
    "build:web": "node build-web.cjs",
    "android:add": "cap add android",
    "android:sync": "npm run build:web && cap sync android",
    "android:open": "cap open android",
    "android:build": "cap build android",
    "web:serve": "npm run build:web && npx serve www"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/core": "^8.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.1"
  }
}
